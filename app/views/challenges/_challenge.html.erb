
<% content_for :instructions do %>
    <!-- Contenido de las instrucciones del reto -->
    <div>
        <h1 class="text-3xl mb-8"> <%= challenge.title %> </h1>
        <p class="my-4 text-md"> <%= challenge.intro %> </p>
        <p class="my-4 text-md"> <%= challenge.premise %> </p>
    </div>
<% end %>

<% content_for :blockly do %>
    <!-- Contenido del juego -->
    <div id="<%= dom_id challenge %>">
        <div id="blocklyDiv" class="h-full w-full" style="height: 100vh; width: 40vw;" >
        </div>
    </div>
<% end %>

<% content_for :game do %>
<p>Test</p>
<% end %>